{
  "name": "koleya-backend",
  "version": "1.0.0",
  "description": "Backend API Koleya ERP PME",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "test": "jest --runInBand --forceExit",
    "setup": "psql -U postgres -h localhost -f setup_db.sql && npm run migrate",
    "migrate": "psql -U koleya -d koleya -f migrations/001_init.sql && psql -U koleya -d koleya -f migrations/002_notifications.sql && psql -U koleya -d koleya -f migrations/003_seed_demo.sql && psql -U koleya -d koleya -f migrations/004_abonnements_softdelete.sql"
  },
  "dependencies": {
    "2": "^3.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "supertest": "^7.2.2",
    "uuid": "^9.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  }
}
